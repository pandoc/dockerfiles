#
# Python filters
#
# We are freezing only the 2 first digits (e.g. `1.3`) 
# because minor versions (e.g. `1.3.6`) _should_ not break things
#

pandoc-codeblock-include==1.1.*
pandoc-latex-environment==1.2.*
pandoc-include==1.4.*
